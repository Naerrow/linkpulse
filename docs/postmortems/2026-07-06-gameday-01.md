# GameDay 01 기록지 — P3-2 의도적 장애 주입 (드릴 당일 이 문서를 채운다)

- 계획·예측 원문: [`docs/plans/0003-p3-2-fault-injection.md`](../plans/0003-p3-2-fault-injection.md) §4
- runbook(따라 할 문서): [`docs/runbooks/alarm-response.md`](../runbooks/alarm-response.md)
- 역할: **주입·복구(mutating) = 사람** / 관측·기록 = 에이전트(read-only) — 가드레일 #1
- 드릴 일자: **2026-07-06 (KST)** — 07-03 착수 시도는 chaos 이미지 ECR 미존재로 연기, 07-06 재개
- 상태: **드릴 완료(S1·S2·S3 전부 실측, 2세트)** — **1차 세트**(13:31~15:35, 사용자가 세션 밖에서 직접 실행, Claude는 사후 재구성)는 C-1 로그 부재·S1 중복 실행·S2 첫 시도 무효 등 절차 공백이 있었음. 사용자 요청으로 **2차 세트(16:08~16:32, Claude 실시간 관측)**를 처음부터 재실행 — C-1 연속 폴링·정확한 T0·run id 고정까지 전부 갖춘 **공식 판정 근거**. 아래 P1~P9 판정은 2차 세트 기준이며, 1차 세트는 참고·비교용으로 남긴다. **회고 확정 완료**([`2026-07-06-gameday-01-retro.md`](2026-07-06-gameday-01-retro.md), 2026-07-06 — A-1~A-10 전수 판정).

> 시각은 전부 **KST, 초 단위까지**(`date "+%H:%M:%S"`) 기록한다. MTTD/MTTR 산출 근거다.

---

## 0. 사전 게이트 (하나라도 미충족이면 드릴 시작 금지)

2026-07-03 13:23~13:24 KST 게이트 재실측 + 2026-07-06 드릴 당일 재확인(에이전트 read-only) 결과를 아래에 기입.
**P3-1 PR 머지는 게이트에서 제외** — 사용자 결정(2026-07-03): push·머지는 P3 전체 완료 후 일괄, 기준은 사용자가 정한다. 드릴은 main의 `deploy.yml`(P2 머지분)·이미 apply된 알람 스택·ECR 이미지로 진행되므로 머지 여부와 기술적으로 무관. 드릴 산출물은 현 브랜치(`infra/p3-cloudwatch-slack-alerts`)에서 계속 작업.

- [x] **Slack 배선**: 콘솔 OAuth → `terraform.tfvars`에 `slack_team_id`/`slack_channel_id` → apply(사람) → 확인:
  `aws sns list-subscriptions-by-topic --topic-arn $(terraform -chdir=infra/prod output -raw sns_alarms_topic_arn) --region ap-northeast-2` → 구독 1개(https/chatbot)
  — **실측 13:23: 구독 1개(https, `global.sns-api.chatbot.amazonaws.com`)** ✓
- [x] **Slack 수신 실증**: `set-alarm-state` 테스트(ALARM→OK, `infra/README.md` 절차)로 카드 실수신 확인
  — **실측(07-03) 11:34:09 OK→ALARM / 11:34:24 ALARM→OK(`alb-target-5xx`)** ✓ — **07-06 드릴 당일 재확인**: 별도 `set-alarm-state` 없이 드릴 자체(S1·S2)에서 발생한 실제 알람 6건이 전부 Slack에 실수신됨(사용자 스크린샷 확인) — 실질적으로 더 강한 재실증
- ~~**P3-1 PR 머지**~~ → **게이트 제외**(위 사용자 결정 — 드릴 산출물 브랜치 분리도 P3 완료 후 사용자가 판단)
- [x] **스택 가동**: `curl -sk -o /dev/null -w '%{http_code}\n' https://lpulse.live/healthz` = 200 — **실측 13:24(07-03): 200**, **07-06 드릴 후 재확인: 200** ✓
- [x] **chaos 이미지 준비 완료**: `load/chaos/README.md` 4단계(빌드→로컬 404 검증→push→**arm64 확인**) 전부 통과
  — 07-03에 1~2단계(로컬 검증) 완료, **07-06 10:19:15 KST ECR push 완료**(`imageDigest: sha256:1ba9bcfe...`, 사용자 실행) — `describe-images`로 존재 확인(에이전트 read-only, 07-06)
- [x] **B-1 기준선 기록**(아래 표, 07-06 재실측) — **이것 없이 S1 시작 금지**(자동 롤백이 지연·실패하는 순간 즉시 필요)
- [x] **B-2**: 알람 12개 전부 OK 확인(치트시트 C-2) — **실측 13:23(07-03): 12/12 OK**, **07-06 드릴 후 재확인: 12/12 OK** ✓ + Slack 수신 실증 완료
- [x] **드릴 창 동안 `terraform apply`/`destroy` 금지 합의** — 특히 `full-destroy-prod.sh`는 **알람 이력·로그·ECR을 지워 드릴 증거가 소실**된다(§5) — 드릴 진행 중 apply/destroy 실행 없음(사용자 확인)

### B-1 — 수동 롤백 기준선 (드릴 당일 재실측 필수)

참고: 2026-07-03 실측값과 07-06 재실측값이 동일 — taskdef `linkpulse-prod-app:10`, 이미지 태그 `7d5f88073ec4040bb80408ffb1af6aa916766e69`(스택 무변경 지속). **당일 값이 다르면 당일 값이 기준.**

| 항목 | 기록(당일 2026-07-06) |
| --- | --- |
| 기록 시각 | 2026-07-06 13:2x KST(드릴 전, 07-03 값과 동일 확인) / 드릴 후 재확인 15:56 KST |
| 서빙 taskdef ARN·리비전 (C-3 ①) | `linkpulse-prod-app:10` · desired 2 / running 2 · PRIMARY `COMPLETED`(드릴 종료 후에도 동일 리비전으로 복귀) |
| 이미지 태그 = known-good (C-3 ②) | `7d5f88073ec4040bb80408ffb1af6aa916766e69` |
| ECR에 그 태그 존재 확인 (C-3 ③) | [x] 확인됨 |
| chaos-healthz-v1 ECR 존재·arm64 | [x] **push 완료** — 2026-07-06 10:19:15 KST, `imageDigest: sha256:1ba9bcfe051f2ae9205b0c572d0a7d943f5400f4d512bc32ed9abc7df723d4f5`, `describe-images`로 존재 확인(arm64는 로컬 buildx 빌드 시 `--platform linux/arm64` 명시로 보장, 배포 결과가 정상적으로 헬스체크 실패 유형이었던 것 자체가 아키텍처 일치의 간접 증거 — exec format error 크래시가 아니었음) |

---

## 1. 명령 치트시트 (드릴 중 복붙용 — C-* 관측은 read-only, I-*/R-*는 사람 실행. **R-* 번호는 runbook과 동일 체계**)

```bash
# C-1 외부 관측(별도 터미널에 상시): 5초 간격 healthz 폴링 + 타임스탬프
while true; do echo "$(date '+%H:%M:%S') $(curl -sk -o /dev/null -w '%{http_code}' --max-time 4 https://lpulse.live/healthz)"; sleep 5; done

# C-2 알람 상태 전수(비정상만)
aws cloudwatch describe-alarms --alarm-name-prefix linkpulse-prod \
  --query 'MetricAlarms[?StateValue!=`OK`].[AlarmName,StateValue]' --output table --region ap-northeast-2

# C-3 기준선: ①서빙 taskdef → ②이미지 태그 → ③ECR 존재
aws ecs describe-services --cluster linkpulse-prod-cluster --services linkpulse-prod-app \
  --query 'services[0].taskDefinition' --output text --region ap-northeast-2
aws ecs describe-task-definition --task-definition <위 ARN> \
  --query 'taskDefinition.containerDefinitions[0].image' --output text --region ap-northeast-2
aws ecr describe-images --repository-name linkpulse/app --image-ids imageTag=<태그> --region ap-northeast-2

# C-4 서비스 이벤트·배포 상태(S1의 1차 증거 — rolloutState/rolloutStateReason 포함)
aws ecs describe-services --cluster linkpulse-prod-cluster --services linkpulse-prod-app \
  --query 'services[0].{desired:desiredCount,running:runningCount,
  deployments:deployments[].{id:id,status:status,rollout:rolloutState,reason:rolloutStateReason,taskDef:taskDefinition,failed:failedTasks},
  events:events[0:10].[createdAt,message]}' --output json --region ap-northeast-2

# C-5 타깃 헬스
aws elbv2 describe-target-health --region ap-northeast-2 --target-group-arn \
  $(aws elbv2 describe-target-groups --names linkpulse-prod-tg --query 'TargetGroups[0].TargetGroupArn' --output text --region ap-northeast-2)

# C-6 알람 이력(드릴 후 증거 덤프에도 사용)
aws cloudwatch describe-alarm-history --alarm-name <알람명> --history-item-type StateUpdate \
  --start-date <드릴시작 ISO> --end-date <드릴종료 ISO> --output json --region ap-northeast-2

# I-1 [사람] S1 주입: main에서 chaos 태그 배포
#   ⚠ dispatch 전 진행 중 run 없는지 확인(1차 드릴서 중복 실행됨 — 회고 A-7)
gh run list --workflow=deploy.yml --branch main -L 3          # in_progress 없어야 함
gh workflow run deploy.yml --ref main -f image_tag=chaos-healthz-v1
gh run list --workflow=deploy.yml --branch main --event workflow_dispatch -L 5 \
  --json databaseId,createdAt,status,url  # createdAt이 위 dispatch 시각 이후인 run이 방금 것 — id 고정·기록(직전 dispatch 오인 방지)
gh run watch <id>   # run 링크·resolved 액션 버전(SHA)을 기록지에 남긴다

# I-2 [사람] S2 주입: 실다운
aws ecs update-service --cluster linkpulse-prod-cluster --service linkpulse-prod-app \
  --desired-count 0 --region ap-northeast-2

# I-3 [사람] S2: 먼저 첫 503을 기다린 뒤(드레인 창 ~30s 동안 200 혼입 방지) 503 연사 12발
#     계획 §4 원문 순서 그대로 — "첫 503 확인 후 연사". 503이 10건 이상이어야 P7 버킷 분할 보장 성립.
until [ "$(curl -sk -o /dev/null -w '%{http_code}' --max-time 4 https://lpulse.live/)" = "503" ]; do sleep 2; done
echo "first 503: $(date '+%H:%M:%S')"
for i in $(seq 1 12); do echo "$(date '+%H:%M:%S') $(curl -sk -o /dev/null -w '%{http_code}' --max-time 4 https://lpulse.live/)"; sleep 2; done

# R-1 [사람] S1 수동 롤백(정식 경로 — 여유 있을 때만. = runbook R-1): B-1의 known-good 태그 재배포
#     주의: chaos run이 deploy.yml concurrency(직렬화)를 점유한 동안 이 dispatch는 큐에 밀린다.
gh workflow run deploy.yml --ref main -f image_tag=<B-1의 태그>

# R-1e [사람] S1 긴급 롤백(사용자 영향 진행 중 — GitHub 우회, 즉시. = runbook R-1e): B-1의 taskdef ARN 직접 지정.
#      새 deployment가 chaos deployment를 즉시 대체. 점유 중이던 run은 red로 끝난다(정상).
aws ecs update-service --cluster linkpulse-prod-cluster --service linkpulse-prod-app \
  --task-definition <B-1의 taskdef ARN> --region ap-northeast-2
# 이후 큐 run 정리: gh run list --workflow=deploy.yml → 필요 시 gh run cancel <id>

# R-2 [사람] S2 복구: desired 복원 (= runbook R-2)
aws ecs update-service --cluster linkpulse-prod-cluster --service linkpulse-prod-app \
  --desired-count 2 --region ap-northeast-2
```

---

## 2. S1 — 나쁜 이미지 배포 (자동 방어 검증, 무중단 예상)

**중단 기준**: C-1에서 200 아닌 응답 관측 또는 HealthyHostCount<2 → **즉시 R-1e**(긴급, GitHub 우회 — R-1 dispatch는 chaos run이 concurrency를 점유한 동안 큐에 밀리므로 긴급용이 아니다).

### 타임라인

**1차 세트(참고— C-1 로그 없이 사후 재구성, 판정에는 2차 세트를 우선 사용)**: 사용자가 세션 밖에서 I-1을 직접 실행했고, Claude는 사후에 `describe-services`/`describe-alarm-history`로 ECS·CloudWatch 이력을 재구성했다. **S1이 두 번 실행됨**(연속 두 차례 dispatch, 원인 미상). 두 시도 모두 동일한 패턴(헬스체크 404 반복 실패 → circuit breaker 감지 → 자동 롤백)으로 귀결됐으나 C-1 연속 폴링 로그가 없어 무중단(P1)을 정량적으로 판정할 수 없었다.

**1차 시도** (deployment `ecs-svc/3537486304761507585`):

| 시각(KST) | 이벤트 | 출처 |
| --- | --- | --- |
| 13:31:24 | 첫 chaos 태스크 시작 (**T0**) | C-4 재구성 |
| 13:33:07 | "unable to stop or start tasks... Update minimumHealthyPercent" 경고 | C-4 |
| 13:33:28~13:37:05 | 태스크 반복 unhealthy(404)·중단·재시도(circuit breaker 자동 태스크 교체 포함) | C-4 |
| 13:36:21 | `alb-unhealthy-hosts` Slack ALARM | CloudWatch alarm history + Slack(사용자 확인) |
| 13:37:09.779 | ECS 이벤트 "(deployment ...) deployment failed: tasks failed to start." | C-4 원문 |
| 13:37:09.780 | ECS 이벤트 "rolling back to deployment ecs-svc/6964196269323777054." | C-4 원문 |
| 13:39:41.406 | 롤백 deployment 완료(COMPLETED), steady state | C-4 |
| 13:40:19 | `alb-no-healthy-hosts` Slack ALARM (attempt 1 회복 직전에 발화 — 평가 지연) | CloudWatch alarm history |
| 13:42:19 / 13:42:21 | `alb-no-healthy-hosts` / `alb-unhealthy-hosts` OK 복귀 | CloudWatch alarm history |

**2차 시도** (deployment `ecs-svc/9106623745363835163`, 원인: 사용자가 재실행 — 사유 확인 필요):

| 시각(KST) | 이벤트 | 출처 |
| --- | --- | --- |
| 13:46:55 | 첫 chaos 태스크 시작 | C-4 |
| 13:49:08~13:53:37 | 태스크 반복 unhealthy(404)·중단 | C-4 |
| 13:53:41.864 | "(deployment ...) deployment failed: tasks failed to start." | C-4 원문 |
| 13:53:41.865 | "rolling back to deployment ecs-svc/6964196269323777054." | C-4 원문 |
| 13:54:40.117 | 롤백 deployment 완료(COMPLETED), steady state | C-4 |

2차 시도에서는 알람이 발화하지 않았다(CloudWatch 이력 조회 결과 해당 구간 없음) — 1차의 쿨다운·평가 상태 잔류 또는 실패 사이클 패턴 차이로 추정.

**2차 세트(공식 판정 근거 — Claude 실시간 관측, C-1 폴링·run id 고정 포함)**: T0 이전인 16:08:21부터 C-1(healthz 5초 폴링)이 이미 돌고 있었고, run id(`28774201504`, `gh run list --branch main --event workflow_dispatch`로 dispatch 직후 고정)도 확정했다. 이번엔 **단 한 번만 실행**됐다.

| 시각(KST) | 이벤트 | 출처 |
| --- | --- | --- |
| 16:09:41 | `gh workflow run` dispatch 접수(run `28774201504`) | `gh run list` |
| 16:10:20 | 첫 chaos 태스크 시작 (**T0**) | C-4 실시간 관측 |
| 16:10:20~16:16:52 | 태스크 반복 unhealthy(404)·중단·재시작 사이클 | C-4 |
| 16:16:52.677 | ECS 이벤트 "(deployment ecs-svc/7905389329879164546) deployment failed: tasks failed to start." | C-4 원문 |
| 16:16:52.678 | ECS 이벤트 "rolling back to deployment ecs-svc/6964196269323777054." | C-4 원문 |
| 16:17:55.348 | 롤백 deployment 완료(COMPLETED), steady state (T0+7분35초) | C-4 |
| (드릴 후) | Actions run `28774201504` 결론 확인 = **`failure`** | `gh run view` |
| (드릴 후) | C-1 로그: 16:08:21~16:19:48 구간(S1 전체 포함) **200 아닌 응답 0건** | C-1 로그(`evidence/gameday-01/c1-healthz.log`) |
| (해당 구간) | 알람: 발화 없음(CloudWatch 이력 조회 결과 없음) | CloudWatch alarm history |

- MTTD/MTTR 해당 없음(S1은 자동 롤백 실증 — 사람의 감지·대응 없음)
- T0→failed 6분32초, T0→완료 7분35초 — 1차 세트 두 시도(8분17초·7분45초)와 일관되게 예측 범위(8~15분) 하한부

### 예측 vs 실측 (판정: 적중/부분/빗나감 — 2차 세트 기준, 1차 세트는 참고 병기)

| # | 예측(계획 §4 요약) | 실측 | 판정 |
| --- | --- | --- | --- |
| P1 | 무중단: C-1 내내 200, HealthyHostCount 2 유지 | **2차 세트**: C-1 연속 로그로 확인 — 16:08:21~16:19:48 구간 200 아닌 응답 **0건**(정량 증거 확보). 1차 세트는 로그 없이 ECS 이벤트 패턴(min100%/max200% 롤링으로 구 태스크 유지)만으로 정성 추정했었음 | **적중**(2차 세트로 정량 확정) |
| P2 | `alb-unhealthy-hosts` ALARM→Slack (가능성 높음, 단정 아님) | 2차 세트: 미발화. 1차 세트 1차 시도에서는 13:36:21 발화(Slack 실수신 확인), 1차 세트 2차 시도·2차 세트 모두 미발화(3회 중 1회만 발화) | **부분**(계획서도 "단정 아님"이라 명시 — 3회 중 1회 발화로 예측 폭 안에 들어옴, "확률적" 성격이 실측으로 뒷받침됨) |
| P3 | 실패 태스크 3개째서 FAILED+자동 롤백, T0+8~15분. 1차 증거=C-4 이벤트·rolloutStateReason | 2차 세트: T0(16:10:20)→failed(16:16:52)=6분32초, 완료(16:17:55)=7분35초. 1차 세트 두 시도(8분17초·7분45초)까지 **총 3회 전부 자동 롤백 성공**, 이벤트 원문 3세트 확보 | **적중**(3회 재현, 예측 범위 하한부 근접) |
| P4 | run **red**(보조 신호). 에러 문구·resolved 버전·판정 경로 기록. green이면 A-1 재론 | 2차 세트 run `28774201504` **failure** — 에러 `Deployment ecs-svc/7905389329879164546 not found after stabilization. The deployment was likely rolled back by the deployment circuit breaker.`, resolved `amazon-ecs-deploy-task-definition@v2`=SHA `c465972…`, **판정 경로 = 부재 감지**(waiter timeout 아님 — 계획 §2가 예측한 경로 그대로). 1차 세트 두 run(`28768439062`·`28767917966`)도 둘 다 failure | **적중**(3회 전부 red, 판정 경로까지 예측대로 — "부재 감지") |
| P5 | 침묵: 5xx 계열·no-healthy-hosts·running-tasks-low | 2차 세트: 완전 침묵(알람 이력 없음). 1차 세트 1차 시도에서만 `alb-no-healthy-hosts`가 회복 직전(13:40:19)에 발화 — 3회 중 1회만 이례적 | **적중**(2차 세트 기준. 1차 세트의 1회성 이례는 아래 참고에 남김) |

### 증거 칸 (원문 붙여넣기)

**2차 세트(공식)**:
- ECS 이벤트: `(deployment ecs-svc/7905389329879164546) deployment failed: tasks failed to start.` / `rolling back to deployment ecs-svc/6964196269323777054.`
- 롤백 deployment: `rolloutState=COMPLETED` / `reason=ECS deployment ecs-svc/6964196269323777054 completed.`
- Actions run: [`28774201504`](https://github.com/Naerrow/linkpulse/actions/runs/28774201504) / conclusion **failure** / 에러문구(07:23:03 UTC): `Deployment ecs-svc/7905389329879164546 not found after stabilization. The deployment was likely rolled back by the deployment circuit breaker.` / resolved 액션 `aws-actions/amazon-ecs-deploy-task-definition@v2` SHA `c465972ecbd160473f22e683363b422a5412a3de` / **판정 경로 = 부재 감지**(deployment가 circuit breaker 롤백으로 목록에서 사라짐 → 액션이 throw). 로그 원본 `evidence/gameday-01/s1-run-28774201504.log`
- C-1 폴링 요약: 16:08:21~16:19:48 구간 전부 200(비200 0건) — `evidence/gameday-01/c1-healthz.log`

**1차 세트(참고)**:
- ECS 이벤트(1차 시도): `(deployment ecs-svc/3537486304761507585) deployment failed: tasks failed to start.` / `rolling back to deployment ecs-svc/6964196269323777054.`
- ECS 이벤트(2차 시도): `(deployment ecs-svc/9106623745363835163) deployment failed: tasks failed to start.` / `rolling back to deployment ecs-svc/6964196269323777054.`
- Actions run: [`28768439062`](https://github.com/Naerrow/linkpulse/actions/runs/28768439062)(failure) / [`28767917966`](https://github.com/Naerrow/linkpulse/actions/runs/28767917966)(failure)

---

## 3. S2 — 전체 다운 드릴 (감지→대응 리허설, 실다운 5~10분)

**전제**: S1 종료 후 안정 상태(알람 전부 OK, healthy 2) 재확인.
**MTTD** = T0(I-2 실행) → 첫 Slack ALARM 수신. **MTTR** = 첫 Slack ALARM → healthz 연속 200 회복(복원 후). OK 통지 시각은 별도 병기.

### 타임라인

**1차 세트(참고)**: 첫 시도(13:57:35~13:58:34)는 무효(22초 만에 복원, 알람 평가 시간 없음). **재시도(15:44:31~15:56:25)**가 1차 세트 안에서의 유효 실측 — I-3 없이 자연 트래픽만으로 503 발생, MTTD 4분54초(`alb-elb-5xx`)/MTTR 4분11초(총 다운타임 9분5초), `alb-no-healthy-hosts`·`ecs-running-tasks-low`가 **복구 완료 이후에야** 발화하는 이례적 패턴 관측(§ 하단 참고 박스).

> **1차 세트 참고 각주(판정 무관)**: 알람 이력에 `alb-target-5xx`가 14:04:05→14:04:08 사이 **약 3초** ALARM으로 떴다 기록됨(어느 시나리오와도 시간대가 겹치지 않는 유휴 구간 — S1 2차 종료 13:54 이후, S3 15:34 이전). 정체 불명의 순간 발화로, P9(target-5xx 침묵) 판정에는 영향 없음(P9는 S2 구간 기준). 완전성 차원의 기록.

**2차 세트(공식 판정 근거 — Claude 실시간 관측, I-3 503 연사 포함)**:

| 시각(KST) | 이벤트 | 출처 |
| --- | --- | --- |
| 16:19:48 | I-2 실행, 태스크 2개 중단 (**T0**) | C-4 실시간 |
| 16:19:53 | 첫 503 관측(C-1) | C-1 로그 |
| 16:19:53~16:26:41 | 연속 503(총 **81건**, C-1 5초 간격 폴링 기준) | C-1 로그 |
| 16:23:25.974 | `alb-elb-5xx` Slack ALARM (**MTTD 기준**, 5분간 16건→27건 누적) | CloudWatch alarm history + Slack 스크린샷(사용자 확인) |
| (16:26:03 이전) | R-2 실행(desired=2) | C-4(태스크 시작 시각 역산) |
| 16:26:03 | 신규 태스크 2개 시작 | C-4 |
| 16:26:40 | 타깃 그룹 재등록 | C-4 |
| 16:26:41 | 마지막 503 관측(C-1) — 이후 200 지속 | C-1 로그 |
| 16:26:59 | steady state 도달 (**MTTR 기준**) | C-4 실시간 |
| (해당 구간) | `alb-no-healthy-hosts`·`ecs-running-tasks-low` | **발화 없음** — 다운 지속시간(7분11초)이 1차 세트(9분5초)보다 짧아 두 알람의 평가창을 못 채운 것으로 추정 |
| 16:44:25.975 | `alb-elb-5xx` OK 복귀 — **복구(16:26:59)로부터 17분26초 지연** | CloudWatch alarm history + Slack 스크린샷(사용자 확인) |
| (드릴 후) | desired=2/running=2, healthz 200, 알람 12/12 OK | C-4·C-2·헬스체크(에이전트 재확인) |

- **MTTD = 3분 37초**(T0 16:19:48 → 첫 ALARM 16:23:25.974)
- **MTTR = 3분 34초**(첫 ALARM 16:23:25 → steady state 16:26:59) — 프로젝트 정의(ALARM→정상, §3 상단·TEMPLATE.md)
- **총 다운타임 = 7분 11초**(T0 16:19:48 → steady state 16:26:59 = MTTD 3분37초 + MTTR 3분34초) — 사용자 영향 지속 시간, MTTR과 구분
- **회복 통지(OK) 지연 = 17분 26초**(복구 16:26:59 → OK 16:44:25) — `alb-elb-5xx`는 감지(MTTD 3분37초)는 빠르나 OK는 매우 늦음. 원인: `HTTPCode_ELB_5XX_Count`가 **sparse 카운트 메트릭**(5xx 0일 때 "0"이 아니라 데이터포인트 부재)이라, 복구 후 missing→`notBreaching` 확정에 CloudWatch가 오래 걸림(OK 카드 문구 "no datapoints were received for 1 period ... treated as NonBreaching"). **1차 세트에선 같은 알람 OK가 복구 후 2분49초 만에 왔음(15:53:36→15:56:25)** — 회복 통지 지연이 2분~17분으로 편차 큼(sparse 메트릭 특성, 벽시계 버킷 경계 의존 추정). → **A-9**
- I-3 없이도(사용자가 별도 curl 연사 안 함) C-1의 5초 폴링만으로 503 81건 확보 — 10건 기준 압도적으로 충족

### 예측 vs 실측 (2차 세트 기준, 1차 세트 이례는 참고로 병기)

| # | 예측(계획 §4 요약) | 실측 | 판정 |
| --- | --- | --- | --- |
| P6 | `alb-no-healthy-hosts`: ~4–6분 내 ALARM→Slack (breaching 설계의 실전 검증) | **2차 세트**: 미발화(다운 7분11초 만에 복구돼 평가창 못 채움). **1차 세트**: 9분48초(복구 후)에야 발화 — 두 세트 모두 예측(4~6분)과 다름 | **빗나감**(2회 모두 예측 이탈 — 이 알람은 예측만큼 빠르지 않거나, HealthyHostCount 지표 자체의 평가 지연이 있음. A-5로 원인 규명 필요) |
| P7 | `alb-elb-5xx`: ≥5/5분 → ALARM (~5–7분, 버킷 경계 시 +5분) | 2차 세트: T0+3분37초 발화(1차 세트는 +4분54초) — **두 세트 모두 이 알람이 실제 MTTD를 담당**, 예측 범위 하한부 | **적중**(2회 재현 — "MTTD 기준 알람"이 계획서 예측의 no-healthy-hosts가 아니라 elb-5xx로 실증됨, 회고 핵심 발견) |
| P8 | `ecs-running-tasks-low`: ALARM **또는** 지표 단절로 침묵 | 2차 세트: 미발화(다운 시간 짧아 평가창 못 채움). 1차 세트: 복구 완료 17초 후 발화 | **부분**(다운 지속시간에 따라 발화 여부가 갈림 — 7분대는 침묵, 9분대는 발화하지만 항상 늦음. A-6 유지: 짧은 다운에서도 늦게라도 울릴지, 아예 안 울릴지 경계가 다운 시간에 좌우됨을 명시) |
| P9 | 침묵: target-5xx·RDS 계열 | 2차 세트도 확인 — 해당 알람 이력 없음(2회 재현) | **적중** |

---

## 4. S3 (선택) — 태스크 1개 강제 종료 (자가치유 관찰)

- [사람] 태스크 1개 강제 종료 실행됨(2회, 아래) → 예측: ~2분 내 보충, **알람 전부 침묵**(3분 평가창보다 빠른 자가치유)
- **1차 세트**: T0 15:34:50(draining 시작) / 보충 완료 15:35:38(steady state, 약 48초) / 알람 없음
- **2차 세트(공식)**: T0 16:31:33(draining 시작) / 보충 완료 16:32:13(steady state, **약 40초**) / 알람 없음(해당 구간 CloudWatch 이력 없음, 신규 발화 재확인)
- **판정: 적중**(2회 재현, 40~48초 — 자가치유가 알람 평가창(3분)보다 훨씬 빨라 완전히 조용히 처리됨)

---

## 5. 증거 수집 (드릴 직후, 같은 날 — **full-destroy 전에 반드시**)

`full-destroy-prod.sh`는 CloudWatch 알람(이력 포함)·로그·ECR 이미지를 지운다. **회고 확정 전 destroy 금지.**
`docs/postmortems/evidence/`는 **.gitignore 대상(로컬 보관 전용)** — 원본 JSON·캡처에는 계정 ID·ARN·Slack ID 같은 운영 메타데이터가 들어간다. 커밋되는 회고 문서에는 **내용을 확인한 발췌만** 싣는다.

- [x] C-1 healthz 폴링 로그 — `evidence/gameday-01/c1-healthz.log`(16:08:21~16:35:15, 318행, PID 기록·정상 종료)
- [x] C-4 서비스 이벤트 폴링 로그(2차 세트) — `evidence/gameday-01/s1-rerun-ecs-poll.log`·`s2-recovery-poll.log`·`s3-poll.log`
- [x] C-4 서비스 이벤트·deployments JSON 원본 — `evidence/gameday-01/ecs-service-events.json`(S1 롤백 이벤트 3건 포함)
- [x] Actions run 확인 — 2차 세트 `28774201504`(failure), 1차 세트 `28768439062`·`28767917966`(둘 다 failure) — `gh run view`/`gh run list`로 조회, §2 증거 칸에 반영. **run 로그 원본** `evidence/gameday-01/s1-run-28774201504.log`(에러문구·resolved SHA `c465972…`·판정경로=부재감지 포함)
- [x] Slack 알람 카드 스크린샷 — 2차 세트: `alb-elb-5xx` ALARM(16:23) + **OK(16:44) 캡처 확인**(OK 카드에 "no datapoints ... NonBreaching" 문구 = A-9 근거). 1차 세트 3건 기캡처
- [x] C-6 알람 이력 JSON 원본 덤프 — `evidence/gameday-01/alarm-history-full.json`(드릴 전체 04:00~08:00 UTC 상태변화 14건, **MTTD 근거 재현성 확보 — full-destroy 대비**)
- [x] 회고 초안 작성 — `docs/postmortems/2026-07-06-gameday-01-retro.md`(TEMPLATE.md 양식, 액션 아이템 A-1~A-10 전수 판정)

### 액션 아이템 후보 (드릴 직후 관측 근거 — **최종 전수 판정(A-1~A-10, 반영/백로그/기각)은 회고 [`2026-07-06-gameday-01-retro.md`](2026-07-06-gameday-01-retro.md) §8**)

- ~~A-4(C-1 폴링 필수화)~~ → **이번 2차 세트에서 즉시 해소**: C-1을 드릴 시작 전부터 백그라운드로 걸어 S1 무중단(P1)을 정량 확정함. 액션 아이템이 아니라 "다음 드릴부터 표준 절차로 채택"만 남음.
- **A-5**: `alb-no-healthy-hosts`가 S2 2회(1차 세트 9분48초/복구후, 2차 세트 미발화) 모두 예측(4~6분)과 다르게 동작 — HealthyHostCount 지표의 평가 지연 또는 "타깃 0개"(deregister) 상태에서의 메트릭 발행 방식 자체를 원인으로 조사 필요
- **A-6**: `ecs-running-tasks-low`(Container Insights 기반)는 다운 지속시간이 길 때만(9분대) 늦게 발화하고 짧으면(7분대) 아예 침묵 — runbook에 "이 알람 자체를 실시간 다운 감지용으로 신뢰하지 말 것, 뜨면 먼저 `describe-services`로 현재 상태부터 재확인"을 명시
- **A-7**: S1이 1차 세트에서 두 번 dispatch된 원인은 확인 안 됨(재발 방지책 필요 여부는 사용자 판단) — 단, 2차 세트에서 run id를 dispatch 직후 즉시 `gh run list`로 고정하는 절차를 적용해 재발해도 어느 run인지 식별 가능하도록 개선함
- **A-8(신규)**: `alb-elb-5xx`가 S2의 실질적 MTTD 알람 역할(2회 재현, 3분37초·4분54초)을 하는데도 계획서 예측(P6)은 `no-healthy-hosts`를 MTTD 기준으로 지목했음 — runbook·온콜 가이드의 "S2 발생 시 가장 먼저 볼 알람"을 `alb-elb-5xx` 우선으로 정정
- **A-9(신규)**: `alb-elb-5xx`의 **회복 통지(OK) 지연** — 감지는 빠르나(MTTD 3~5분) OK는 복구 후 2분~17분으로 편차 크게 늦게 옴(sparse 5xx 카운트 메트릭의 missing→notBreaching 확정 지연). runbook·온콜 가이드에 "복구 확인은 healthz 200·`describe-services`로 직접 할 것, OK 알림을 복구 완료 신호로 기다리지 말 것" 명시
